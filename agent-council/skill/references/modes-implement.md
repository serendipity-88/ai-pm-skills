# Mode: Implement

用于把已选方案拆成可执行计划。

Legacy alias: `work`。

## 默认边界

外部 actor 默认不直接改文件，只输出:

- 文件修改计划
- 接口设计
- patch 草案
- 测试策略
- 风险点

如果要让 actor 直接改文件，必须显式定义:

- ownership
- 可改文件
- 只读文件
- 验收命令
- merge/冲突处理

## 输出

- 修改范围
- 步骤顺序
- 文件级计划
- 测试/验证命令
- 不应修改的范围
- 回滚方案

